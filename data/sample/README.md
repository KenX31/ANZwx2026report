# Sample Data

These CSV files are synthetic fixtures for UI smoke tests only. They preserve the production column layout but do not contain real merchants, transaction values, cities, or operational data.

Use `DATA_BACKEND=sample` for local UI checks without access to the private data repository.
